# Linux-Specific Configuration
# Only apply on Linux systems

# To enable: keep filename as-is
# To disable: rename to 01-workstation-linux.conf.disabled

Host *
    # Linux-specific SSH agent socket
    # Uncomment if you use a custom agent location
    # IdentityAgent ~/.ssh/agent.sock

    # Alternative: Use systemd user socket
    # IdentityAgent $XDG_RUNTIME_DIR/ssh-agent.socket

    # Most Linux systems work fine with global defaults
    # This file exists for Linux-specific overrides as needed
